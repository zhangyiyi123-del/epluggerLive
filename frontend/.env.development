# 开发时使用空值，请求走 Vite 代理到 http://localhost:8080（手机用局域网 IP 访问时也必须留空，否则图片会错误指向手机上的 localhost）
VITE_API_BASE_URL=
# 留空则与 API 同源（相对路径 /api/...），局域网调试可正常加载图片。若本机调试且坚持直连后端可设为 http://localhost:8080（仅 PC 用 localhost 打开前端时有效）
VITE_UPLOADS_BASE_URL=
